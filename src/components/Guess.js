import React from 'react';

const Guess = () => {
    return (
        <div className="guess">
            <h2>Já sei a palavra!</h2>
            <input></input>
            <button>Chutar</button>
        </div>
    );
};

export default Guess;